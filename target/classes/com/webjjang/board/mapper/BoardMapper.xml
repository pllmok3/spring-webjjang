<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.webjjang.board.mapper.BoardMapper">
	
	<select id="list" resultType="com.webjjang.board.vo.BoardVO">
		select * from (
			select rownum rnum,no,title,writer,writeDate,hit
			from(
				select no,title,writer,writeDate,hit
				from board 
				<include refid="search" />
				order by no desc
			)
		)
		where rnum between #{startRow} and #{endRow}
	</select>
	
	<select id="getCount" resultType="java.lang.Integer">
		select count(*) from board
		<include refid="search" />
	</select>
	
	<sql id="search">
		<!-- 검색어가 존재하는 경우에만 검색을 실행한다. : mybytis 문서 참조해야함. -->
		<if test="word != null and word !=''.toString()  ">
		  where ( 2 = 1
		   <!-- Mybatis에서 2글자 이상인 경우 : String 취급,
		     한 글자인 경우:char 취급 => toString()를 이용해서 문자열로 만듬-->
		   <if test="key == 't'.toString()">
		    <!-- concat(문자열, 문자열) : 2개만 연결,
		      "||" : 문자열 이어 붙이는 연산자 -> concat(a, concat(b,c) ) -->
		       or title like '%'||#{word}||'%'
		   </if>
		   <if test="key == 'c'.toString()">
		          or content like '%'||#{word}||'%'
		   </if>
		   <if test="key == 'w'.toString()">
		          or writer like '%'||#{word}||'%'
		   </if>
		   <if test="key == 'tc'.toString()">
		          or title like '%'||#{word}||'%'
		          or content like '%'||#{word}||'%'
		   </if>
		   <if test="key == 'tw'.toString()">
		          or title like '%'||#{word}||'%'
		          or writer like '%'||#{word}||'%'
		   </if>
		   <if test="key == 'cw'.toString()">
		          or content like '%'||#{word}||'%'
		          or writer like '%'||#{word}||'%'
		   </if>
		   <if test="key == 'tcw'">
		          or title like '%'||#{word}||'%'
		          or content like '%'||#{word}||'%'
		          or writer like '%'||#{word}||'%'
		   </if>
			)
		</if>
	</sql>
	
	<!-- 게시판 글보기 -->
	<select id="view" resultType="com.webjjang.board.vo.BoardVO">
		select userId,no,title,content,writer,writeDate,hit
		from board
		where no = #{no}
	</select>
	
	<!-- 조회수 1 증가 -->
	<update id="increase">
		update board set hit = hit +1
		where no=#{no}
	</update>
	
	<!-- 게시판 글수정 -->
	<!-- 비밀번호가 틀릴경우 오류가 나지않으면서 정보수정이 x -->
	<update id="update">
		update board
		set title =#{title}, content=#{content}, writer = #{writer}
		where no = #{no}
	</update>	
	
	
	<insert id="write">
		insert into board(no,userId,title,content,writer)
		values(board_seq.nextval,#{userId},#{title},#{content},#{writer})
	</insert>
	
	<delete id="delete">
		delete from board where no=#{no}
	</delete>
	
	<!-- 회원등록 시키기 -->
	<insert id="gaip">
		insert into member 
		values(#{userId},#{userPw})
	</insert>
	
	<select id="loginCheck" resultType="int">
		select count(*) from member where userId = #{userId}
		<if test="userPw != null ">
			and userPw = #{userPw}
		</if>
	</select>
	
</mapper>